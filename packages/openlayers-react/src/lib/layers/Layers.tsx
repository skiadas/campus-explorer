import { ReactElement, ReactNode } from 'react';
export const Layers = ({ children }: { children?: ReactNode }): ReactElement => {
  // eslint-disable-next-line react/jsx-no-useless-fragment
  return <>{children}</>;
};
